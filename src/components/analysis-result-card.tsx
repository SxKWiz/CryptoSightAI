import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Separator } from "@/components/ui/separator";
import { Badge } from "@/components/ui/badge";
import type { AnalysisResult } from "@/types";
import { ArrowDownRight, Target, XCircle, ShieldCheck, Signal } from "lucide-react";
import { cn } from "@/lib/utils";

interface AnalysisResultCardProps {
  result: AnalysisResult;
}

export function AnalysisResultCard({ result }: AnalysisResultCardProps) {
  const { analysisSummary, tradeSignal, riskLevel, confidenceLevel } = result;

  const getRiskColor = (level: typeof riskLevel) => {
    switch (level) {
      case "Low":
        return "bg-green-500/10 text-green-400 border-green-500/20";
      case "Medium":
        return "bg-yellow-500/10 text-yellow-400 border-yellow-500/20";
      case "High":
        return "bg-red-500/10 text-red-400 border-red-500/20";
      default:
        return "bg-secondary text-secondary-foreground";
    }
  };

  return (
    <Card>
      <CardHeader>
        <div className="flex flex-col md:flex-row md:items-start md:justify-between gap-4">
          <div>
            <CardTitle>AI Analysis & Trade Signal</CardTitle>
            <CardDescription>
              Generated by Gemini. This is not financial advice.
            </CardDescription>
          </div>
          <div className="flex items-center gap-4 shrink-0">
             <Badge variant="outline" className={cn("text-sm", getRiskColor(riskLevel))}>
              <Signal className="h-4 w-4 mr-1.5" />
              {riskLevel} Risk
            </Badge>
            <Badge variant="outline" className="text-sm bg-blue-500/10 text-blue-400 border-blue-500/20">
              <ShieldCheck className="h-4 w-4 mr-1.5" />
              {confidenceLevel} Confidence
            </Badge>
          </div>
        </div>
      </CardHeader>
      <CardContent className="space-y-6">
        <div>
          <h3 className="text-lg font-semibold mb-2">Analysis Summary</h3>
          <p className="text-muted-foreground">{analysisSummary}</p>
        </div>

        <Separator />

        <div>
          <h3 className="text-lg font-semibold mb-4">Trade Signal</h3>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
            <div className="flex items-start space-x-3">
              <div className="bg-blue-500/10 text-primary p-2 rounded-full">
                <ArrowDownRight className="h-5 w-5" />
              </div>
              <div>
                <p className="text-sm text-muted-foreground">Entry Price</p>
                <p className="font-semibold">{tradeSignal.entryPriceRange}</p>
              </div>
            </div>
            
            <div className="flex items-start space-x-3">
              <div className="bg-green-500/10 text-green-500 p-2 rounded-full">
                <Target className="h-5 w-5" />
              </div>
              <div>
                <p className="text-sm text-muted-foreground">Take Profit Levels</p>
                <div className="flex flex-wrap gap-2 mt-1">
                  {tradeSignal.takeProfitLevels.map((level, index) => (
                    <Badge key={index} variant="secondary" className="bg-green-500/10 text-green-400 border-green-500/20">
                      {level}
                    </Badge>
                  ))}
                </div>
              </div>
            </div>

            <div className="flex items-start space-x-3">
              <div className="bg-red-500/10 text-red-500 p-2 rounded-full">
                <XCircle className="h-5 w-5" />
              </div>
              <div>
                <p className="text-sm text-muted-foreground">Stop Loss</p>
                <p className="font-semibold">{tradeSignal.stopLoss}</p>
              </div>
            </div>
          </div>
        </div>
      </CardContent>
    </Card>
  );
}
